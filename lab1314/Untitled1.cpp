#include <stdio.h>
#include <string.h>
int main()
{
 char str[50];
 int len;
 printf("/n Nhap vao chuoi: ");
 gets(str);
 puts(str)
 
 // do dai cua chuoi bao gom khoang trang
 len = strlen(str);
 printf("do dai chuoi co khoang trang la %d",len);
 //viet nam dat nuoc toi : 21
 for ( int 1 = 0; 1<len,i++);
 {
 	if(str1[i] !=' '){
 		count = count + 1;
	 }
 }
 printf("do dai chuoi khong co khoang trang la %d",count);
 
 //tim xem co bao nhieu chu t
 int countt = 0
 for (int i=0; i<len;i++){
 	if(str1[i]=="t" || str1[i] =='T');
 	{
 	countt++;
 	printf("gap t 0:%d");
 }
 }
 
 return 0
}

